gcp_project_id = "replace-with-gcp-project-id"
aws_account_id  = "123456789012"

# Set this when AWS Academy does not allow creating a dedicated IAM role.
# lab_role_arn = "arn:aws:iam::123456789012:role/LabRole"

# Leave false until container images have been built and pushed.
deploy_compute = false
